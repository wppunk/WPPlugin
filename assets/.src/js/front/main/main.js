/**
 * Class Main.
 *
 * @since {VERSION}
 */
export class Main {

	/**
	 * Main constructor.
	 *
	 * @since {VERSION}
	 */
	constructor() {
		// eslint-disable-next-line no-console
		console.log( 'Main was started' );
	}
}
