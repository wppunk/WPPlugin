/**
 * Class Main.
 *
 * @since {VERSION}
 */
class Main {
	/**
	 * Main constructor.
	 *
	 * @since {VERSION}
	 */
	constructor() {
		// eslint-disable-next-line no-console
		console.log( 'Main was started' );
	}
}

export default Main;
